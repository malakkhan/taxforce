{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "python3 run_dashboard.py --server.address 0.0.0.0 --server.port $PORT"
    }
}